pipeline {
    agent any
    stages {
		stage('Gitclone') {
                    steps {
                        sh 'rm -r Practest'
                        sh 'git clone https://github.com/kianwi/ssd-test'
                    }
                }
			
                }
}